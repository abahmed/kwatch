{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch kwatch",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceRoot}/cmd/kwatch",
            "showLog": true,
            "env": {
                "CONFIG_FILE": "${workspaceRoot}/config.yaml"
            }
        }
    ]
}