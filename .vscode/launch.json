{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch kwatch",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceRoot}",
            "showLog": true,
            "env": {
                "CONFIG_FILE": "config.yaml"
            }
        }
    ]
}